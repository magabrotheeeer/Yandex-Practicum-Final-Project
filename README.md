# Файлы для итогового задания

# ToDo Scheduler

Простой веб-сервер для создания, просмотра, поиска и управления задачами с хранением данных в SQLite.

## Задачи со звездочкой

Выполнены все задачи со звездочкой кроме авторизации и dockerfilek

## Переменные окружения

Скопируйте `.env.example` в `.env` и при необходимости измените:

TODO_PORT = :7540

WEB_DIR = ./web

TODO_DBFILE = scheduler.db



## Сборка и запуск локально

1. Установите Go (версия 1.18+).
2. Склонируйте репозиторий и перейдите в корень проекта.
3. Создайте `.env`:
cp .env.example .env


4. Соберите приложение:
go build -o todo .

5. Запустите сервер:
./todo

6. Откройте в браузере:
- http://localhost:7540/ — главный интерфейс

## Запуск автотестов

Тесты находятся в директории `tests/`. Запустите все тесты:

go test ./tests

Или отдельные:

go test -run ^TestTasks$ ./tests

go test -run ^TestDone$ ./tests


При необходимости включить флаги поиска/авторизации отредактируйте `tests/settings.go`.

## Фронтенд

Все статические файлы (HTML, CSS, JS) лежат в папке `web/`.  
Путь к ней управляется переменной `WEB_DIR`.

---

Этот проект предоставляет простой интерфейс для планирования задач с использованием Go и SQLite.